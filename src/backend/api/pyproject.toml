[project]
name = "carpark-iot-api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]"
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"